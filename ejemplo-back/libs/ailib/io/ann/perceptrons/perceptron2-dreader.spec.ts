import { Perceptron2DReader } from './perceptron2-dreader';

describe('Perceptron2DReader', () => {
  it('should create an instance', () => {
    expect(new Perceptron2DReader()).toBeTruthy();
  });
});
