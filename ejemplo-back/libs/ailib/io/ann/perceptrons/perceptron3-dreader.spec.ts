import { Perceptron3DReader } from './perceptron3-dreader';

describe('Perceptron3DReader', () => {
  it('should create an instance', () => {
    expect(new Perceptron3DReader()).toBeTruthy();
  });
});
