import { IntegrationLayerReader } from './integration-layer-reader';

describe('IntegrationLayerReader', () => {
  it('should create an instance', () => {
    expect(new IntegrationLayerReader()).toBeTruthy();
  });
});
