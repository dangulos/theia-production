import { ConvolutionalLayerReader } from './convolutional-layer-reader';

describe('ConvolutionalLayerReader', () => {
  it('should create an instance', () => {
    expect(new ConvolutionalLayerReader()).toBeTruthy();
  });
});
