import { ReceptiveFieldReader } from './receptive-field-reader';

describe('ReceptiveFieldReader', () => {
  it('should create an instance', () => {
    expect(new ReceptiveFieldReader()).toBeTruthy();
  });
});
