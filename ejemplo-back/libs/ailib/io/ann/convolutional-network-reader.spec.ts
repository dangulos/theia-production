import { ConvolutionalNetworkReader } from './convolutional-network-reader';

describe('ConvolutionalNetworkReader', () => {
  it('should create an instance', () => {
    expect(new ConvolutionalNetworkReader()).toBeTruthy();
  });
});
