import { ConvolutionalNetwork } from './convolutional-network';

describe('ConvolutionalNetwork', () => {
  it('should create an instance', () => {
    expect(new ConvolutionalNetwork()).toBeTruthy();
  });
});
